services:
  tusremesasya:
    container_name: tusremesasya
    image: ghcr.io/gabrielbaute/tus-remesas-ya:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${API_PORT:-8010}:${API_PORT:-8010}"
    volumes:
      - ./instance:/instance
      - ./logs:/logs
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${API_PORT:-8010}/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s